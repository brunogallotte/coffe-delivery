import styled from "styled-components"

export const FormContent = styled.main`
    display: flex;
    flex-direction: column;
    gap: 1rem;

    .inputContainer {
        display: flex;
        gap: 0.75rem;
    }
`